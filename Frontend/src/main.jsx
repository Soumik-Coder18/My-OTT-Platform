import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Root render using React 18's createRoot
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
